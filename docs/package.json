{
  "name": "slidev-addon-subtitle-docs",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev",
    "prebuild": "rm -rf public/examples && yarn workspace slidev-addon-subtitle-example run export && mkdir -p public/examples && cp ../examples/basic/slides-with-subtitles.pdf public/examples/basic.pdf",
    "build": "vitepress build",
    "preview": "vitepress preview"
  },
  "devDependencies": {
    "@tato30/vue-pdf": "^2.0",
    "vitepress": "^1.6"
  }
}
